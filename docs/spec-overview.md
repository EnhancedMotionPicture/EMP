# EMP.MOV v0.1 overview

EMP.MOV uses a sidecar `.emp.json` file to describe synchronized output cues.

## Supported track types
- haptic
- air
- light

## Core event fields
- `t_ms`
- `dur_ms`
- `intensity`
- `shape`

## Goal
Keep the format open, simple, and device-agnostic.
